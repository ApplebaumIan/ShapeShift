# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ShapeShift' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'SwiftOCR', :git => 'https://github.com/joeboyscout04/SwiftOCR.git', :branch => 'update-pod-swift-version' # replace with original pod after the pull request got merged
  # Pods for ShapeShift
  pod 'AppleWelcomeScreen'
  target 'ShapeShiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ShapeShiftUITests' do
    # Pods for testing
  end

end
